experiment_name: curagent-textcraft
trial_name: curagent-textcraft-synth-qwen3-4b-medium

seed: 343
total_train_epochs: 10
tokenizer_path: ${actor.path}
train_difficulties: [medium]
eval_difficulties: [medium]
num_eval_tasks: 100

cluster:
  n_nodes: 1
  n_gpus_per_node: 8
  fileroot: ${oc.env:AREAL_OUTPUT_ROOT,/tmp/areal/experiments}
  name_resolve:
    type: nfs
    nfs_record_root: ${oc.env:AREAL_NAME_RESOLVE_ROOT,/tmp/areal/name_resolve}

allocation_mode: sglang:d4p1t1+d4p1t1

rollout:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  max_concurrent_rollouts: 8
  queue_size: null
  consumer_batch_size: ${train_dataset.batch_size}
  max_head_offpolicyness: 3
  enable_rollout_tracing: false

gconfig:
  n_samples: 1
  min_new_tokens: 0
  max_new_tokens: 8192
  greedy: false
  temperature: 1.0

workflow_config:
  rollout_config:
    model_name: ${actor.path}
    max_steps: 25
    output_dir: ${cluster.fileroot}/${trial_name}/rollouts
    verbose: true
    train: true
    timeout: 1200
    step_timeout: 300
    inference_params:
      temperature: ${gconfig.temperature}
      top_p: 1.0
      max_completion_tokens: ${gconfig.max_new_tokens}
    curagent_root: ${oc.env:CURAGENT_ROOT,/data2/zhangwenjian/agent/curagent}
    curagent_model_config: platoon/textcraft/configs/areal/curagent_model_api.yaml
    prompt_file: null
    max_depth: 12
    max_concurrent_subagents: 4
    max_subagents_per_agent: 6
    max_run_seconds: 900
    max_observation_chars: 8000
    max_retries: 2
  group_size: 8
  use_subprocesses: false
  leave_one_out_baseline: true
  depth_level_weighting: true
  depth_level_discount_gamma: null
  filter_zero_variance_groups: true

loss_fn_config:
  loss_fn: cispo
  clip_low_threshold: 0.0
  clip_high_threshold: 5.0

actor:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: Qwen/Qwen3-4B-Instruct-2507
  init_from_scratch: false
  disable_dropout: true
  gradient_checkpointing: true
  dtype: bfloat16
  mb_spec:
    max_tokens_per_mb: 60000
  optimizer:
    type: adam
    lr: 3e-6
    weight_decay: 0.017
    beta1: 0.9
    beta2: 0.95
    eps: 1e-8
    lr_scheduler_type: constant
    gradient_clipping: 1.0
    warmup_steps_proportion: 0.001
  group_size: ${gconfig.n_samples}
  eps_clip: 0.2
  eps_clip_higher: 0.26
  temperature: ${gconfig.temperature}
  reward_scaling: 1.0
  reward_bias: 0.0
  kl_ctl: 0.0
  kl_estimator: k1
  ppo_n_minibatches: 1
  recompute_logprob: false
  use_decoupled_loss: false
  behav_imp_weight_cap: null
  dynamic_sampling: false
  reward_norm:
    mean_level: null
    std_level: null
  adv_norm:
    mean_level: null
    std_level: null
  max_new_tokens: ${gconfig.max_new_tokens}
  use_lora: false
  peft_type: lora
  lora_rank: 32
  lora_alpha: 16
  target_modules: [all-linear]

ref:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  path: ${actor.path}
  init_from_scratch: false
  disable_dropout: true
  dtype: ${actor.dtype}
  mb_spec:
    max_tokens_per_mb: 60000
  optimizer: null

sglang:
  model_path: ${actor.path}
  random_seed: ${seed}
  skip_tokenizer_init: false
  dtype: ${actor.dtype}
  max_running_requests: null
  context_length: 40001
  mem_fraction_static: 0.8
  enable_lora: ${actor.use_lora}
  max_lora_rank: ${actor.lora_rank}
  lora_target_modules: ${actor.target_modules}

train_dataset:
  batch_size: 16
  type: rl
  path: ""

valid_dataset:
  batch_size: 100
  type: rl
  path: ""

saver:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: 25
  freq_secs: null

recover:
  mode: auto
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: 1
  freq_steps: 5
  freq_secs: 3600

evaluator:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  freq_epochs: null
  freq_steps: 25
  freq_secs: null

stats_logger:
  experiment_name: ${experiment_name}
  trial_name: ${trial_name}
  fileroot: ${cluster.fileroot}
  wandb:
    mode: online
    project: curagent-textcraft

launcher:
  inference_server_cpus_per_gpu: 32
  inference_server_mem_per_gpu: 32768
  trainer_cpus_per_gpu: 4
  trainer_mem_per_gpu: 32768
